		;	tableName,  lambdaTargetTableName, lambdaChannel, egoCorrectionChannel, activeCondition
#if LAMBDA
		veAnalyzeMap = veFrontTableTbl, lambdaTableTbl, lambdaValue2, egoCorrectionForVeAnalyzeFront, { 1 }
		lambdaTargetTables = lambdaTableTbl, afrTSCustom
#else
		veAnalyzeMap = veFrontTableTbl, afrTableTbl, afr2GasolineScale, egoCorrectionForVeAnalyzeFront, { 1 }
		lambdaTargetTables = afrTableTbl, afrTSCustom
#endif

		; filter =  Name,		"DisplayName", outputChannel, operator, defaultVal, userAdjustable
		filter = minRPMFilter, "Minimum RPM", RPMValue,			<		, 500,		, true

		filter = minCltFilter, "Minimum CLT", coolant,		<		, 60,		, true

		filter = deltaTps, "dTPS", deltaTps		>		, 50,		, true

		filter = VBatt, "VBatt", VBatt		<		, 12,		, true

		filter = minTps, "Minimum TPS", TPSValue,		<		, 1,		, true
		filter = std_Custom

[OutputChannels]
		; front cylinder runs on stftCorrection2 (bank 1, fed by lambdaValue2) per cylinderBankSelect2
		egoCorrectionForVeAnalyzeFront = { 100 + stftCorrection2 }
